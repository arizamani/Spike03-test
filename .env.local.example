# SPIKE-03 needs NO cloud credentials — it reads a local manifest + local GLBs.
# The loader substitutes ${CLOUDFRONT_DOMAIN} in fixtures/manifest.sample.json with this base URL.
LOCAL_ASSET_BASE_URL=http://localhost:5173/assets
